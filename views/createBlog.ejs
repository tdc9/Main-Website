<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('./partials/css/bootstrap.ejs') %>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ionn.</title>
    <link rel="icon" type="image/x-icon" href="https://i.ibb.co/Tx2ScBj6/favicon.jpg">
    <style>
        body {
            background: linear-gradient(135deg, #f8fafc 0%, #e9ecef 100%);
            color: #23272b;
            font-family: 'Segoe UI', 'Roboto', Arial, sans-serif;
        }
        .container {
            background: #fff;
            border-radius: 18px;
            padding: 40px 32px;
            margin-top: 48px;
            box-shadow: 0 8px 32px rgba(60,72,88,0.12), 0 1.5px 4px rgba(60,72,88,0.08);
            max-width: 540px;
        }
        h3 {
            color: #2d3a4a;
            font-weight: 700;
            letter-spacing: 1px;
        }
        .form-label {
            color: #4f8cff;
            font-weight: 600;
            letter-spacing: 0.5px;
        }
        .form-title, .form-control {
            background: #f5f7fa;
            color: #23272b;
            border: 1.5px solid #e0e4ea;
            border-radius: 8px;
            transition: border-color 0.2s, box-shadow 0.2s;
        }
        .form-title:focus, .form-control:focus {
            border-color: #4f8cff;
            box-shadow: 0 0 0 0.15rem rgba(79,140,255,.15);
            background: #fff;
        }
        .form-text {
            color: #7b8a99;
            font-size: 0.95em;
        }
        .btn-primary {
            background: linear-gradient(90deg, #4f8cff 60%, #3762c2 100%);
            border: none;
            border-radius: 8px;
            font-weight: 600;
            letter-spacing: 0.5px;
            padding: 10px 32px;
            box-shadow: 0 2px 8px rgba(79,140,255,0.08);
            transition: background 0.2s, box-shadow 0.2s;
        }
        .btn-primary:hover {
            background: linear-gradient(90deg, #3762c2 60%, #4f8cff 100%);
            box-shadow: 0 4px 16px rgba(79,140,255,0.15);
        }
        @media (max-width: 600px) {
            .container {
                padding: 18px 6px;
                margin-top: 18px;
            }
        }
    </style>
</head>
<body>
    <%- include('./partials/components/nav.ejs') %>
    <div class="container text-center my-5">
        <h3>Create A Blog</h3>
        <% if (typeof error !== "undefined" && error) { %>
            <div class="alert alert-danger my-3"><%= error.message || error %></div>
        <% } %>
        <% if (typeof message !== "undefined" && message) { %>
            <div class="alert alert-success my-3"><%= message %></div>
        <% } %>
        <div class="my-5">
        <form method="post" action="/blog/create">
            <div class="mb-3">
                <label for="coverImage" class="form-label">Cover Image URL</label><br>
                <input type="url" name="coverImage" class="form-control" id="coverImage" placeholder="https://example.com/image.jpg" required>
            </div>
            <div class="mb-3 my-4">
                <label for="title" class="form-label">Title</label><br>
                <input name="title" type="text" class="form-title form-control" id="title" aria-describedby="blogTitle" required>
                <div id="titleHelp" class="form-text">A Title For Your Amazing Blog</div>
            </div>
            <div class="mb-3 my-5">
                <label for="content" class="form-label">Content</label>
                <textarea name='content' id='content' class="form-control" rows="7" required></textarea>
                <div id="contentHelp" class="form-text">The Content of Your Amazing Blog</div>
            </div>
            <button type="submit" class="btn btn-primary my-4">Create</button>
        </form>
        </div>
    </div>
    <%- include('./partials/js/bootstrap.ejs') %>
</body>
</html>